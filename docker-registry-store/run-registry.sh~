#!/bin/bash

docker run \
        -e SETTINGS_FLAVOR=local \
        -e STORAGE_PATH=/registry \
        -p 5000:5000 \
        registry

# docker run -d -t \
#          -p 5000:5000 \
#          registry
